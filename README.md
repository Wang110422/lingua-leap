# Lingua Leap

tạo cho tôi một website học từ vựng Header : Có 1 thanh input dùng để tra cứu , phía bên phải sẽ có 1 nút "Tạo mới"  và  phía bên trái sẽ là icon logo và tên logo

2 . Sidebar trái :

Menu dọc , có các item tương ứng với các mục : Trang chủ , Nhóm học , Thư viện của bạn , Lộ trình học , Quét tài liệu và Đã xóa 

ở dưới cùng sẽ là một icon chuông để thông báo và một avartar viền tròn 

3 . Ở trang chủ 

- Banner chào mừng (khối trên cùng, full-width)

Dòng ngày tháng nhỏ (uppercase)

Dòng chào lớn kèm tên người dùng + emoji vẫy tay

Câu hỏi gợi mở bên dưới

Hàng 3 stat-card nằm ngang, mỗi card gồm:

Icon/emoji nhỏ ở góc trên

Số liệu lớn

Nhãn mô tả nhỏ bên dưới số liệu

- Section "Tiếp tục học"

Tiêu đề section + badge tròn nhỏ ghi số lượng lộ trình

Link "Quản lý Lộ trình →" ở góc phải

Grid 3 card ngang hàng, mỗi card gồm:

Icon môn học trong khung vuông bo góc (góc trái)

Badge độ khó (góc phải, cùng hàng với icon)

Tên lộ trình (đậm)

Dòng tiến độ dạng "x/y mốc đã hoàn thành"

Thanh progress bar

Nút "▶ Học tiếp" full-width bên dưới

-  Section "Thư viện của bạn"

Tiêu đề section

Link "Xem tất cả →" ở góc phải

Grid 2 hàng x 3 cột, mỗi card gồm:

Icon chủ đề trong khung vuông bo góc (góc trái)

Nút "..." (menu tùy chọn) ở góc phải

Tên bộ thẻ (đậm)

Dòng phụ dạng "số thuật ngữ · thời gian cập nhật"

-  Sidebar phải — Widget "Lịch ôn hôm nay"

Icon lịch + tiêu đề widget

Badge nhỏ góc phải dạng "x/y thẻ cần ôn"

Khối preview flashcard bên trong: tên thuật ngữ hiển thị lớn ở giữa

Nút nhỏ "Xem nghĩa" bên dưới, căn giữa

- Sidebar phải — Widget "Cập nhật lớp học"

Icon chuông + tiêu đề widget

Danh sách 3 item, mỗi item gồm:

Avatar tròn (chữ cái đầu)

Nội dung thông báo (có phần tên được in đậm)

Dòng thời gian nhỏ bên dưới nội dung

- Sidebar phải — Widget "Bảng xếp hạng"

Icon cúp + tiêu đề widget

Danh sách xếp hạng, mỗi dòng gồm:

Icon huy chương (cho top 1-3) hoặc số thứ tự (từ hạng 4 trở đi)

Avatar tròn (chữ cái đầu)

Tên người dùng

Icon lửa + số ngày streak (dòng phụ dưới tên)

Điểm số (căn phải, đậm)

Dòng của người dùng hiện tại được đánh dấu nổi bật (nền khác + viền trái)

4. Trang nhóm học 

4.1 Phần trang danh sách nhóm học 

- Header trang:

Icon trong khung vuông bo góc

Tiêu đề trang

Dòng mô tả phụ

Bên phải: 2 nút — "Tham gia bằng mã" (icon liên kết, dạng outline) và "+ Tạo Nhóm học mới" (dạng nút chính)

Card danh sách nhóm học (dạng list item), mỗi card gồm:

Icon/emoji đại diện nhóm

Tên nhóm (dạng link)

Badge mã nhóm kèm icon copy

Dòng mô tả nhóm

Hàng thông tin phụ: icon người + số thành viên, icon sách + số bộ thẻ, tên người tạo/quản lý nhóm ở cuối hàng (căn phải)

4.2 Phần trang chi tiết nhóm học (Tab tài liệu học)

Breadcrumb quay lại (giống trên)

Header thông tin nhóm học (giống trên, gồm icon, tên, badge mã nhóm, mô tả, 2 tab pill)

Section header: tiêu đề + mô tả phụ, bên phải là nút "+ Thêm Thư mục / Bộ thẻ vào Nhóm"

Card khung chứa danh sách:

Tiêu đề nhỏ kèm icon + số lượng tài liệu

Grid các item con, mỗi item có 2 dạng:

Dạng "Thư mục": icon thư mục, tên thư mục, dòng mô tả phụ, số lượng bộ từ vựng bên trong, link "Thư mục CSDL" ở góc phải

Dạng "Bộ thẻ": icon bộ thẻ, tên bộ thẻ, dòng mô tả phụ, link "Bắt đầu học →" ở góc dưới

4.3 Phần trang chi tiết nhóm học (thành viên và mời )

Breadcrumb/nút quay lại: mũi tên + text "Quay lại danh sách nhóm học"

Header thông tin nhóm học:

Icon/emoji đại diện nhóm trong khung vuông bo góc

Tên nhóm học

Badge hiển thị mã nhóm

Dòng mô tả nhóm (phụ, xám)

Bên phải: 2 nút/tab dạng pill: "Tài liệu học (x)" và "Thành viên (x/y)"

Card mời thành viên (banner nổi bật):

Badge nhỏ dạng pill kèm icon (Mời thành viên bằng liên kết)

Tiêu đề lớn

Đoạn mô tả hướng dẫn

Dòng hiển thị sĩ số dạng "x/y người" kèm progress bar

Bên phải: ô hiển thị link (dạng input readonly có icon liên kết) + nút "Sao chép liên kết mời" (icon copy)

Section danh sách thành viên:

Tiêu đề section kèm icon + số lượng thành viên

Text phụ góc phải "Tối đa x thành viên"

Grid card thành viên, mỗi card gồm:

Avatar tròn (chữ cái đầu)

Tên thành viên

Email

Badge vai trò (ví dụ: Trưởng nhóm / Thành viên)

5. Trang thư viện của bạn 

5.1 Trang "Thư viện của bạn" (tab Thư mục)

Header trang:

Icon trang trong khung

Tiêu đề trang

Dòng mô tả phụ

Bên phải: nút "Đồng bộ Google" (dạng outline, icon refresh) + nút "+ Tạo Thư mục mới" (dạng nút chính)

Thanh tab điều hướng dạng pill, gồm 3 tab: "Thư mục (x)", "Nhóm học (x)", "Bộ từ vựng (x)" — mỗi tab có icon riêng, tab đang chọn được highlight

Ô tìm kiếm (góc phải thanh tab): icon kính lúp + placeholder "Tìm kiếm trong Thư viện..."

Grid danh sách thư mục, mỗi card gồm:

Icon/avatar đại diện thư mục (dạng chữ viết tắt hoặc icon folder/emoji) trong khung vuông bo góc

Nút "..." (menu tùy chọn) ở góc phải

Tên thư mục (đậm)

Dòng mô tả phụ

Hàng thông tin dưới cùng (có đường kẻ phân cách phía trên): số bộ thẻ (trái) và số thuật ngữ (phải, đậm)

5.2 Trang chi tiết một thư mục

Breadcrumb: nút quay lại + tên thư mục hiện tại

Header thông tin thư mục:

Icon thư mục trong khung vuông bo góc

Tên thư mục

Dòng mô tả phụ

Badge nhỏ hiển thị số lượng bộ thẻ

Bên phải: 3 nút — "Đồng bộ Google" (outline), "Mở File Google Sheet" (nút màu khác, icon kép), "+ Thêm bộ thẻ vào thư mục" (nút chính)

Section "Danh sách Bộ chủ đề học phần": tiêu đề kèm số lượng

Grid card bộ thẻ, mỗi card gồm:

Icon bộ thẻ trong khung vuông bo góc

Icon thùng rác (góc phải, xóa)

Tên bộ thẻ (đậm)

Dòng phụ hiển thị số thuật ngữ

Đường kẻ phân cách

Link "Bắt đầu học từ vựng" kèm icon sách ở cuối card

5.3 Trang "Thư viện của bạn" (tab Bộ từ vựng)

Header trang (giống Image 1): icon, tiêu đề, mô tả phụ, nút "Đồng bộ Google" + "Tạo Thư mục mới"

Thanh tab điều hướng (giống Image 1), tab "Bộ từ vựng" đang được chọn

Ô tìm kiếm bên phải thanh tab

Grid 3 cột danh sách bộ từ vựng, mỗi card gồm:

Badge nhỏ ở góc trên bên trái chỉ nguồn gốc (tên thư mục chứa nó hoặc "Bộ từ vựng độc lập")

Text phụ góc trên bên phải: số thuật ngữ

Icon bộ thẻ + tên bộ từ vựng (đậm, cùng hàng)

Dòng mô tả phụ

Đường kẻ phân cách

Link "Bắt đầu học" kèm mũi tên ở cuối card

6. Trang lộ trình học 

Header trang

Icon trang + tiêu đề "Lộ trình học của tôi"

Dòng mô tả phụ

Bên phải: nút "+ Tạo lộ trình học mới" (nút chính)

Cột trái — Danh sách lộ trình

Danh sách card lộ trình xếp dọc, mỗi card gồm:

Icon lộ trình trong khung vuông bo góc (trái)

Tên lộ trình (đậm)

Dòng thông tin phụ dạng "x ngày · mức độ"

Icon thùng rác (góc phải, xuất hiện khi hover — xóa lộ trình)

Hàng tiến độ: text "x/y mốc" (trái) và phần trăm hoàn thành (phải, đậm)

Progress bar bên dưới

Card đang được chọn có viền nổi bật bao quanh

Card "+ Tạo lộ trình mới" dạng viền nét đứt, nằm cuối danh sách

Cột phải — Chi tiết lộ trình đang chọn

Card thông tin tổng quan:

Icon lộ trình trong khung vuông bo góc

Tên lộ trình (lớn, đậm)

Dòng mô tả lộ trình

Hàng meta-info gồm 3 mục, mỗi mục có icon riêng: thời lượng (số ngày), số mốc bộ thẻ, mức độ

Hàng tiêu đề "Tiến trình hoàn thành mốc" (trái) + text "x/y mốc hoàn thành (phần trăm)" (phải)

Progress bar tổng thể

Card "Các mốc bộ thẻ trong lộ trình":

Tiêu đề card kèm icon ghim + hướng dẫn phụ ở góc phải ("Nhấp vào mốc để bắt đầu học bộ thẻ")

Danh sách các mốc, mỗi mốc gồm:

Badge số thứ tự mốc (dạng "#1") trong khung vuông bo góc

Tên bộ thẻ (đậm)

Hàng thông tin phụ: icon sách + số thuật ngữ, icon mục tiêu + trạng thái "Học: x/y", "Kiểm tra: x/y"

Icon mũi tên ở góc phải (điều hướng)

Đường kẻ phân cách

Hàng dưới: avatar tròn (chữ cái đầu) + "Tác giả: tên người tạo" (trái), link "Vào học bộ thẻ →" (phải)

7. Trang quét tài liệu 

Icon trang trong khung vuông bo góc

Tiêu đề trang

Dòng mô tả phụ

Bên phải: nút "Chọn tài liệu khác" (icon refresh, dạng outline)

2. Section "Danh sách trang tài liệu đã quét"

Tiêu đề section kèm icon + số lượng trang

3. Card chi tiết trang tài liệu

Badge mã trang (dạng "P1")

Tên file trang (đậm)

Badge góc phải hiển thị số từ vựng trích xuất

Tiêu đề phụ "So sánh Ảnh Gốc vs Ảnh Sau Khi Nhận Diện OCR" kèm icon

Hai khối ảnh song song:

Khối trái: nhãn "Ảnh Trang Gốc" (khung trống/placeholder)

Khối phải: nhãn "Ảnh Kết Quả OCR" (có khung viền đậm nổi bật bao quanh, nội dung ảnh có khoanh vùng nhận diện màu)

4. Card "Chữ đã được nhận diện"

Icon + tiêu đề + ghi chú phụ (dạng có thể chỉnh sửa)

Bên phải: nút "Copy chữ", nút "Tra cứu lại"

Khung textbox lớn hiển thị nội dung văn bản (có tay kéo resize góc dưới, thanh cuộn dọc bên phải)

5. Section "Bộ từ vựng trích xuất bên dưới Trang x (n từ)"

Tiêu đề kèm icon sách

Bên phải: thanh bộ lọc dạng nhóm nút gồm "Tất cả CEFR" và các mức riêng lẻ (A1–C2), cạnh đó là nút "Bỏ chọn hết"

6. Grid card từ vựng (nhiều hàng, 3 cột)

Mỗi card gồm:

Checkbox chọn (góc trái)

Từ vựng/cụm từ (đậm)

Badge cấp độ CEFR (góc phải)

Phiên âm IPA (nếu có)

Badge loại từ (VERB/NOUN/PHRASE/ADVERB/IDIOM...) + nghĩa tiếng Việt

Câu ví dụ (dạng in nghiêng, màu nhạt hơn)

8 Trang đã xóa 

1. Header trang

Icon thùng rác + tiêu đề trang

Dòng mô tả phụ

2. Thanh tab điều hướng dạng pill

3 tab: "Thư mục (x)", "Bộ từ vựng (x)", "Từ vựng (x)" — mỗi tab có icon riêng, tab đang chọn được highlight

Đường kẻ phân cách bên dưới thanh tab

3. Card mục đã xóa

Icon/emoji đại diện mục trong khung vuông bo góc (trái)

Tên mục (đậm)

Dòng mô tả phụ bên dưới tên

Đường kẻ phân cách ngang

Hàng nút hành động (căn phải): nút "Khôi phục" (icon mũi tên hoàn tác, dạng outline) và nút "Xóa hẳn" (icon dấu X trong vòng tròn, dạng outline, màu nhấn cảnh báo)

Đặt vai trò là một nhà thiết kế UI,UX

Đây là thiết kế của web học từ vựng của tôi , những chức năng như này , bạn hãy thiết kế sao cho web dễ nhìn , nhìn chuyên nghiệp , có thể thay đổi cấu trúc sao cho phù hợp , có thể thêm 1 số chức năng sao cho phù hợp hơn


Thiết kế theo phong cách của các hình trong ảnh , font chữ màu sắc đều tương tự

This project was built with [Lovable](https://lovable.dev).

## Build with Lovable

Continue developing this project in the [Lovable editor](https://lovable.dev/projects/ca5059d3-98ad-48af-bd78-e1c1130f05d8).

- **Ship faster**: describe what you want to build and Lovable handles the code.
- **Stay in sync**: every change made in Lovable is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally? You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
